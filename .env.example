# Database (for local development outside of k8s)
DATABASE_URL=postgres://dogwalker:dogwalker_dev_password_123@localhost:5433/dog_walker

# Authentication
JWT_SECRET=dev_jwt_secret_change_in_production_abc123xyz

# Google Maps (optional for dev)
GOOGLE_MAPS_API_KEY=

# Square Payments (optional for dev)
SQUARE_ACCESS_TOKEN=
SQUARE_LOCATION_ID=
SQUARE_ENVIRONMENT=sandbox

# Logging
RUST_LOG=debug,tower_http=debug,sqlx=warn

# Server
HOST=0.0.0.0
PORT=8080

# =============================================================================
# Domain Configuration
# =============================================================================
# Primary domain: offleash.pro
#
# Production URLs:
#   API:        https://api.offleash.pro
#   App:        https://app.offleash.pro (or {tenant}.offleash.pro)
#   Admin:      https://admin.offleash.pro
#   Platform:   https://platform.offleash.pro
#
# Test Environment URLs (offleash.world):
#   API:        https://api.offleash.world
#   App:        https://app.offleash.world
#   Admin:      https://admin.offleash.world
#   Platform:   https://platform.offleash.world
#
# Local Development:
#   API:        http://localhost:8080
#   App:        http://localhost:5173
#   Admin:      http://localhost:5174
#   Platform:   http://localhost:5175
# =============================================================================

# Environment: local | test | production
ENVIRONMENT=local

# Base domain (without subdomain)
BASE_DOMAIN=offleash.pro

# API URL (used by frontend apps)
API_URL=http://localhost:8080

# Frontend URLs (used for CORS, redirects)
APP_URL=http://localhost:5173
ADMIN_URL=http://localhost:5174
PLATFORM_URL=http://localhost:5175

# Support email
SUPPORT_EMAIL=support@offleash.pro

# GitHub Integration (for user feedback -> GitHub Issues)
# Create a personal access token with 'repo' scope at https://github.com/settings/tokens
GITHUB_TOKEN=
# Repository to create issues in (format: owner/repo)
GITHUB_FEEDBACK_REPO=kcirtapfromspace/offleash
