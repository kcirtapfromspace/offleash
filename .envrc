# .envrc - direnv configuration for automatic environment activation
# Install direnv and run `direnv allow` to enable

# Use devenv if available (preferred)
if has devenv; then
  use devenv
# Fallback to devbox
elif has devbox; then
  eval "$(devbox generate direnv --print-envrc)"
# Fallback to flake
elif has nix; then
  use flake
fi

# Load local environment overrides if they exist
dotenv_if_exists .env.local
