# =============================================================================
# Test Environment Configuration
# Domain: offleash.world
# =============================================================================

# Database
DATABASE_URL=postgres://dogwalker:${DB_PASSWORD}@db.offleash.world:5432/dog_walker

# Authentication
JWT_SECRET=${JWT_SECRET}

# Google Maps
GOOGLE_MAPS_API_KEY=${GOOGLE_MAPS_API_KEY}

# Square Payments (Sandbox)
SQUARE_ACCESS_TOKEN=${SQUARE_SANDBOX_TOKEN}
SQUARE_LOCATION_ID=${SQUARE_SANDBOX_LOCATION}
SQUARE_ENVIRONMENT=sandbox

# Logging
RUST_LOG=info,tower_http=info,sqlx=warn

# Server
HOST=0.0.0.0
PORT=8080

# Environment
ENVIRONMENT=test

# Base domain
BASE_DOMAIN=offleash.world

# URLs
API_URL=https://api.offleash.world
APP_URL=https://app.offleash.world
ADMIN_URL=https://admin.offleash.world
PLATFORM_URL=https://platform.offleash.world

# Support (still use production email)
SUPPORT_EMAIL=support@offleash.pro

# CORS allowed origins
CORS_ORIGINS=https://app.offleash.world,https://admin.offleash.world,https://platform.offleash.world
